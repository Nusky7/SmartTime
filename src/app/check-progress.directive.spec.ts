import { CheckProgressDirective } from './check-progress.directive';

describe('CheckProgressDirective', () => {
  it('should create an instance', () => {
    const directive = new CheckProgressDirective();
    expect(directive).toBeTruthy();
  });
});
